import UIKit
final class {{ custom_parameters.name }}ViewController: UIViewController, {{ custom_parameters.name }}View, Uses{{ custom_parameters.name }}Presenter {
    let {{ custom_parameters.name | downcase }}Presenter: {{ custom_parameters.name }}Presenter
    private lazy var moduleView: {{ custom_parameters.name }}ModuleView = {{ custom_parameters.name }}ModuleView()

    init(presenter: {{ custom_parameters.name }}Presenter) {
        {{ custom_parameters.name | downcase }}Presenter = presenter
        super.init(nibName: nil, bundle: nil)
        {{ custom_parameters.name | downcase }}Presenter.setView(self)
    }

    required init?(coder aDecoder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    override func loadView() {
        super.loadView()
        view = moduleView
    }

    override func viewDidLoad() {
        super.viewDidLoad()
        {{ custom_parameters.name | downcase }}Presenter.start()
    }
}
