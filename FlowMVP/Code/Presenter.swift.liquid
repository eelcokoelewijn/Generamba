import Foundation

protocol {{ custom_parameters.name }}View: class {

}

protocol {{ custom_parameters.name }}Presenter {
    func start()
    func setView(_ view: {{ custom_parameters.name }}View)
    func dismiss()
}

protocol Uses{{ custom_parameters.name }}Presenter {
    var {{ custom_parameters.name | downcase }}Presenter: {{ custom_parameters.name }}Presenter { get }
}

class Mixin{{ custom_parameters.name }}Presenter: {{ custom_parameters.name }}Presenter {
    weak var view: {{ custom_parameters.name }}View?
    weak var delegate: {{ custom_parameters.name | downcase }}PresenterDelegate

    func start() {

    }

    func setView(_ view: {{ custom_parameters.name }}View) {
        self.view = view
    }

    func dismiss() {
      delegate?.dismiss(presenter: self)
    }
}
