{% include 'Header' %}

import UIKit
import IGListKit

final class {{ module_info.name }}ViewController: UIViewController {
    lazy var {{ module_info.name | downcase }}View: {{ module_info.name }}View = {
        let view = {{ module_info.name }}View()
        view.translatesAutoresizingMaskIntoConstraints = false
        return view
    }()
    private let {{ module_info.name | downcase }}Controller: {{ module_info.name }}Controller

    @objc init(controller: {{ module_info.name }}Controller) {
        {{ module_info.name | downcase }}Controller = controller
        super.init(nibName: nil, bundle: nil)
        {{ module_info.name | downcase }}Controller.setViewController(self)
    }

    required init?(coder aDecoder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    override func viewDidLoad() {
        super.viewDidLoad()
        setupViews()
        applyViewConstraints()
        {{ module_info.name | downcase }}Controller.start()
    }

    private func setupViews() {
        automaticallyAdjustsScrollViewInsets = false
        view.backgroundColor = .white
        view.addSubview({{ module_info.name | downcase }}View)
    }

    private func applyViewConstraints() {
        NSLayoutConstraint.activate([{{ module_info.name | downcase }}View.topAnchor.constraint(equalTo: topLayoutGuide.bottomAnchor),
                                     {{ module_info.name | downcase }}View.leadingAnchor.constraint(equalTo: view.leadingAnchor),
                                     {{ module_info.name | downcase }}View.trailingAnchor.constraint(equalTo: view.trailingAnchor),
                                     {{ module_info.name | downcase }}View.bottomAnchor.constraint(equalTo: bottomLayoutGuide.topAnchor)])
    }
}